language: ruby
rvm:
  - 2.5.0

sudo: false

install: gem install html-proofer

script: bundle exec jekyll build && htmlproofer ./_site --check_favicon

env:
  global:
  - NOKOGIRI_USE_SYSTEM_LIBRARIES=true

cache: bundler
